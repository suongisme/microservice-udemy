server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/ms_employee
    username: root
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
  application:
    name: EMPLOYEE-SERVICE

eureka:
  instance:
    client:
      serverUrl:
        defaultZone: http://localhost:8761/eureka